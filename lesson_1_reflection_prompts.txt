Question: How did viewing a diff between to versions of a file help you see the bug that was introduced?

Answer: It helped me focus only on the few lines that were changed.

Question: How could having easy access to the entire history of a file make you a more efficient programmer in the long term?

Answer: You can go back to previous versions of a file if something worked better.

Question: What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google Docs does?

Answer: By manually committing versions will be more complete and logical.

Question: Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?

Answer: Files in Git are interrelated while files in Google Docs are generally not.

Question: How can you use the commands git log and git diff to view the history of files?

Answer: git log shows you all the different versions of a file with the changes that they went through and how big the changes are. git diff caompares two files more closely so that you can determine exactly what has changed.

Question: How might using version control make you more confident to make changes that could break something?

Answer: If I break something I can just go back to an earlier version.

Question: Now that you have your workspace set up, what do you want to try using Git for?

Answer: To keep track of the different versions of my program.